language: generic
install:
  - sudo apt-get install -y python-pip
  - sudo pip install flake8
script:
  #- set -euo pipefail
  # TODO: Fix check-helpers.sh and remove the || true
  - ./scripts/check-helpers.sh || true
  - ./scripts/py-style-check.sh
